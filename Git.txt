Git - window64비트 다운

repository = 여기에 저장? 프로젝트 단위인듯

GitBash // git 명령어 입력 프로그램

명령어들

cd = change directory

git init = git 초기화 file directory에 .git 파일생성

git remote add oring ( 주 소 ) = ( 주 소 )에 올린다고 선언? hmm..

git add = git에 올린다.

git log


